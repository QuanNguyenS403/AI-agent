# Normative bootstrap policy. Numeric business thresholds require Owner decisions.
version: "1.0.0"
environment: offline
dry_run: true
external_publishing: false
paid_acquisition: false
ads_budget_vnd: 0
capital_gate: FAIL
unknown_evidence: DENY
unknown_rights: DENY
self_approval: DENY
legacy_config: QUARANTINE
ai_disclosure: evaluate_current_policy_per_asset
claim_requirement: verified_scoped_current_evidence
runtime_policy: ../../ai-brand-company-os/config/policy.json
# Runtime reads the central JSON policy; this YAML is human-readable documentation.
