# Offline bootstrap: no values or credentials are needed.
# This file is documentation, not a switch that activates a live transport.
DRY_RUN=true
EXTERNAL_PUBLISHING=false
PAID_ACQUISITION=false
# Future deployments use an independent secret manager and authorized service identity.
# Never paste credentials into this repo, logs, CLI arguments or chat.
